//  Math =  built-in object that provides a
//          collection of propertioes and methods

// Math.PI

// console.log(Math.PI)
// console.log(Math.E)

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x);                   // Rrumbullakso
// z = Math.floor(x);                   // Rounds Down - Posht
// z = Math.ceil(x);                    // Rounds Up
// z = Math.trunc(x);                   // Eliminate decimal portions
// z = Math.pow(y , x);                 // Y to the Power of X
// z = Math.sqrt(x);                    // Square root
// z = Math.log(x);                     // Logaritm
// z = Math.sin(x);                     // Sin of X
// z = Math.cos(x);                     // Cosin of X
// z = Math.tan(x);                     // Tangent of X
// z = Math.abs(x);                     // Absolute Value of negative is always positive in absolute value ~~~ `abs` - "absolute"
// z = Math.sign(x);                    // Returns 1 & -1 & 0
// let max = Math.max(x , y , z);       // Max sum of 3 numbers
// let min = Math.min(x, y, z);         // Min sum of 3 numbers
console.log(max);